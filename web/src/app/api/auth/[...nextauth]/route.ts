// Auth.js v5 — handlers pour /api/auth/* (signin, callback, signout, etc.).
// Cf. doc/p2-pivot-webapp.md chantier 4 et ADR-014.

import { handlers } from '@/lib/auth/auth';

export const { GET, POST } = handlers;
